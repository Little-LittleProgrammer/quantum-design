// import type { Plugin } from 'vite';

// import imagemin from 'unplugin-imagemin/vite';
// import { ViteEnv } from '../types';

// 开启gzip、br压缩
export function vite_plugin_imagemin(env: ViteEnv): Plugin | Plugin[] {
    // if (env.VITE_USE_IMAGEMIN) {
    //     return [imagemin()];
    // }
    // return [];
}

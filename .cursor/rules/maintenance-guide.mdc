---
alwaysApply: false
---
# quantum-design - 知识工程维护指南

## 🎯 维护原则

### 1. 非必要不修改知识，仅在AI发挥不佳时进行修剪

```tips
我们非常清楚，经常让你来维护知识是个非常困难的事儿(我们自己也是如此🥲)。因此，我们希望知识工程尽量的轻量化，以免让它成为你的负担。
```

建议多用AI来实现需求，并且不要花费过多的注意力在知识的维护和更新上。即使项目随着需求的不断迭代变得更大、更复杂，但只要AI尚且能让你觉得算是"好用"，你就不需要去维护知识。

当发现AI的回答中明显有理解错误的地方，此时再针对AI误解或不清楚的地方，对这些特定的知识进行维护即可。

**常见需要修剪的情况：**
- AI 经常混淆不同工作区的开发模式
- AI 不理解 monorepo 的构建依赖顺序
- AI 违反类型安全原则或性能要求
- AI 忽略 quantum-design 特有的架构设计

### 2. 将固定/成熟的开发流程写进知识中

```tips
将某些已经非常固定的流程写入知识中，能够节省你大量的开发时间。 
```

举个例子，在实现前端业务的过程中，我们经常会新增一些页面/组件等功能，而正常我们让AI帮我们写这部分代码时，它只会帮你写组件代码。而对应的路由配置、菜单配置、权限配置等需要你在他回答完成后再次对它进行提醒，它才会帮你做这部分工作。

对于这种任务，你可以直接在知识中将这个固定的模式告知它，**让它每次在实现新的页面/组件时，也顺便参考现有配置，帮你把这些配置文件完成**，这种固定流程的知识能显著减少你提醒AI的次数。

**quantum-design 项目中的固定流程：**
- **新增组件**：同时更新组件库导出、添加 playground 演示、编写 README 文档
- **发布流程**：使用 Changesets 添加变更记录、更新版本、发布到 npm
- **构建顺序**：基础包 → 样式包 → 组件库 → 应用层的依赖构建
- **CLI 工具**：支持 --help 参数、模板验证、跨平台兼容性检查

### 3. 让AI了解你项目中的那些"坑"

有些项目因为历史原因，可能有一些坑，比如构建时需要特定的环境变量才能构建成功，或是某些组件库版本有特殊的配置要求，当你发现AI经常卡在这些地方无法继续时，我们建议你将这些坑写在知识中告诉AI，这能避免它浪费大量的时间在这些卡点上。

比如AI每次帮你实现完需求都会构建/打包一下应用来检查代码是否正确，但你的项目必须配置某些环境变量或构建参数才能成功，这会导致AI以为代码有问题，尝试去修改正确的代码，这是我们不希望看到的🥲

**quantum-design 项目的已知"坑"：**
- **Tauri 环境变量**：`TAURI_ENV_PLATFORM` 影响构建路径配置
- **pnpm catalog**：依赖版本要使用 `catalog:` 而不是具体版本号
- **workspace 协议**：内部包依赖必须使用 `workspace:*`
- **Vite 7 升级**：某些插件配置与 Vite 6 不兼容
- **CSS Variables**：主题系统依赖 CSS 变量，不能直接使用 SCSS 变量

## 📖 知识定义与范围

知识就是一段经验、一段记忆，在研发流程中的知识包括以下这些:

### 1. 专业技能经验
- **前端基础建设经验**：monorepo 架构设计、构建工具链、组件库开发
- **Vue 3 生态经验**：Composition API、Pinia、Vue Router、组件设计模式
- **工程化经验**：Vite、Turbo、TypeScript、测试策略、性能优化

### 2. 项目代码结构
- **monorepo 工作区划分**：apps、packages、configs、cli 的职责分工
- **依赖关系图**：基础包 → 样式包 → 组件库 → 应用层的构建依赖
- **类型系统分层**：全局类型、组件类型、业务类型的分离原则

### 3. 项目开发规范
- **代码规范**：TypeScript 类型安全、命名约定、文件组织
- **组件设计规范**：API 设计原则、样式系统、可复用性原则
- **性能规范**：千万级用户的性能要求、优化策略

### 4. 业务领域知识
- **前端基础建设**：脚手架设计、构建配置、开发工具链
- **组件库设计**：API 设计、主题系统、可扩展性
- **工程化最佳实践**：版本管理、发布流程、质量保障

### 5. 团队共识
- **技术选型共识**：为什么选择 Vite 7、Turbo、pnpm、Changesets
- **架构设计共识**：monorepo 的分层设计、类型安全原则
- **开发流程共识**：spec 工作流、质量检查、发布流程

### 6. 其他重要信息
- **环境配置**：Node.js、pnpm 版本要求、Volta 版本管理
- **部署策略**：GitHub Pages 静态部署、npm 包发布
- **监控集成**：Sentry 错误监控、性能监控策略

## 🔄 知识文件更新策略

### 项目知识工程 (project-knowledge.mdc)
**更新时机**：当项目架构、技术栈发生重大变化时更新
- 新增或移除主要技术栈（如升级 Vue 版本、替换 UI 库）
- 工作区结构调整（新增 packages 或重组目录）
- 核心业务定位变化（扩展新的业务领域）

### 开发工作流程 (development-workflow.mdc)
**更新时机**：当团队工作流程优化或调整时更新
- spec 工作流程的改进和调整
- monorepo 构建流程的优化
- 版本管理和发布流程的变更

### 项目规范 (project-standards.mdc)
**更新时机**：当代码规范调整或新增最佳实践时更新
- TypeScript 配置升级带来的类型规范变化
- ESLint 规则集的更新和调整
- 性能优化策略的补充和完善
- 新的命名约定或组件设计原则

### 开发集成规范 (integration-standards.mdc)
**更新时机**：当平台集成方式或部署流程变化时更新
- 构建工具升级（Vite、Turbo 版本更新）
- CI/CD 流程的改进
- 第三方服务集成的变更（Sentry、GitHub Pages）

### 开发质量检查清单 (quality-checklist.mdc)
**更新时机**：当发现新的质量问题或改进检查机制时更新
- 发现新的常见问题和解决方案
- 质量检查工具的升级（ESLint、TypeScript、Vitest）
- 性能基准和监控指标的调整

### 知识工程维护指南 (maintenance-guide.mdc)
**更新时机**：当维护策略或最佳实践发生变化时更新
- 发现新的项目"坑"和解决方案
- 知识维护流程的优化
- 团队协作模式的改进

## 📊 知识文件效果评估

### 评估指标
定期评估各知识文件的使用效果：

1. **AI理解准确性**
   - AI是否能正确识别不同工作区的开发任务
   - 是否遵循正确的构建依赖顺序
   - 是否理解类型安全和分层架构原则

2. **开发效率提升**
   - 减少重复说明相同规范的次数
   - 提高首次生成代码的质量
   - 缩短从需求到可运行代码的时间

3. **代码质量保障**
   - 生成的代码是否符合项目规范
   - 是否避免常见的性能和安全问题
   - 测试覆盖率和构建成功率

4. **团队协作顺畅度**
   - 不同开发者使用AI的一致性
   - 代码审查中发现的规范问题数量
   - 新人上手项目的速度

### 评估方法
- **每月回顾**：总结AI开发中遇到的问题和改进点
- **版本发布后**：评估知识文件对发布质量的影响
- **团队反馈**：收集开发者对AI协作效果的反馈
- **工具监控**：通过构建成功率、测试覆盖率等客观指标评估

### 持续改进
基于评估结果持续优化知识工程：
- 补充AI经常理解错误的规范点
- 更新过时的技术栈和工具信息
- 完善新发现的最佳实践和避坑指南
- 简化过于复杂或低效的规范条目

---

## 🎉 结语

知识工程初始化完成后，这只是第一步。一个项目我很难一次性将里面的所有代码看一遍，以后我们在实现业务的过程中，你要多让小七来做，如果小七哪里做的不好，你就把小七理解错误的地方写在知识工程的文件里，这样小七下次在遇到相同问题时有的参考，就不会犯同样的错误了。

**那么，享受你的开发之旅吧！** 🚀

---
alwaysApply: false
---
# quantum-design - 开发工作流程规范

## 🎯 智能判断标准

根据需求智能判断使用哪种模式开发，默认采用 spec 流程：

- **使用 spec**：新功能开发、复杂架构设计、多模块集成、组件库扩展、CLI工具开发、构建配置修改
- **跳过 spec**：简单修复、文档更新、样式调整、类型定义补充、单元测试编写

## 🎮 Workflow 命令控制

**可用命令：**
- **/spec** - 强制使用完整 spec 流程
- **/no_spec** - 跳过 spec 流程，直接执行
- **/help** - 显示命令帮助

## 📝 Spec 工作流程

每个环节完成后都需要用户确认后才可进行下一个环节：

### 1. 需求分析阶段
- 如果是新需求，按照标准软件工程方式独立开展工作
- 明确是组件库开发、CLI工具、构建配置还是演示应用需求
- 搞清楚问题和需求，然后再进入下一阶段
- 需要时使用 interactiveDialog 工具收集信息

### 2. 需求文档和验收标准设计
- 按照 EARS 简易需求语法方法来描述
- 针对 quantum-design 项目特点：
  - 组件库需求：明确组件API设计、样式规范、交互行为
  - CLI工具需求：明确命令参数、文件生成规则、模板结构
  - 基础库需求：明确函数签名、类型定义、使用场景
- 如果涉及前端页面，需要在需求中提前确定好设计风格和配色（遵循现有主题系统）
- 必须与用户确认需求细节，最终确认清楚后需求定稿
- 保存在 `specs/spec_name/requirements.md` 中

```markdown
# 需求文档

## 介绍
需求描述，明确属于 quantum-design 哪个工作区

## 需求

### 需求 1 - 需求名称

**用户故事：** 用户故事内容

#### 验收标准

1. 采用 EARS 描述的子句 While <可选前置条件>, when <可选触发器>, the <系统名称> shall <系统响应>
2. 对于组件库：组件必须支持 TypeScript 类型定义，遵循 API 设计原则
3. 对于CLI工具：必须支持 --help 参数，提供清晰的使用文档
4. 必须通过 ESLint 和 TypeScript 类型检查
5. 必须包含单元测试覆盖主要功能
...
```

### 3. 技术方案设计
- 根据 quantum-design 的技术架构和确认的需求，进行技术方案设计
- 确定影响的工作区包（packages/、configs/、cli/等）
- 明确依赖关系和版本兼容性
- 对于组件库：确定组件层次、样式方案、测试策略
- 对于CLI工具：确定模板结构、文件生成逻辑、依赖管理
- 对于配置包：确定配置文件结构、插件集成、兼容性处理
- 必要时使用 mermaid 绘制架构图或流程图
- 保存在 `specs/spec_name/design.md` 中

### 4. 任务拆分
- 根据需求文档和技术方案，细化具体要做的事情
- 按照 monorepo 工作区进行任务分组
- 考虑任务依赖关系和构建顺序
- 明确每个任务的验收标准和测试要求
- 保存在 `specs/spec_name/tasks.md` 中
- 执行时尽可能独立自主运行，保证效率和质量

任务参考格式：

```markdown
# 实施计划

## 基础库任务
- [ ] 1. 更新 packages/utils 工具函数
  - 添加新的工具方法
  - 更新类型定义
  - 添加单元测试
  - 更新文档
  - _需求: 相关的需求点编号

## 组件库任务  
- [ ] 2. 开发 packages/vue3-antd-pc-ui 新组件
  - 实现组件核心逻辑
  - 编写组件样式 (SCSS + CSS Variables)
  - 定义 TypeScript 类型
  - 编写单元测试
  - 在 playground 中添加演示
  - _需求: 相关的需求点编号

## 构建配置任务
- [ ] 3. 更新 configs/vite 构建配置
  - 修改 Vite 插件配置
  - 更新类型定义
  - 测试构建流程
  - _需求: 相关的需求点编号
```

## 🔄 monorepo 开发特殊流程

### 包依赖管理
- 使用 `pnpm add <package> --filter=<workspace>` 添加依赖
- 内部包使用 `workspace:*` 协议
- 版本管理使用 catalog 模式统一管理

### 构建顺序
- 基础包优先：shared → types → utils → hooks
- 样式系统：styles → tailwind
- 组件库：vue3-pc-ui → vue3-antd-pc-ui
- 配置包可并行构建
- 应用层最后：playground、yapi-to-typescript

### 测试策略
- 单元测试：使用 Vitest，每个包独立测试
- 集成测试：在 playground 中验证组件集成
- 类型测试：使用 vue-tsc 进行类型检查
- 构建测试：验证所有包能正确构建和发布

### 发布流程
- 使用 @changesets/cli 管理版本和变更日志
- 遵循语义化版本规范
- 确保所有包构建成功且测试通过
- 发布到 npm registry 前先进行预发布验证

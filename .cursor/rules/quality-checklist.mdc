---
alwaysApply: false
---
# quantum-design - 开发质量检查清单

## ✅ 开发前检查步骤

### 1. 【必选】场景识别
- **[ ] 明确工作区类型**：确定当前是哪个工作区的开发任务
  - 基础库开发（packages/utils、shared、types、hooks）
  - 组件库开发（packages/vue3-antd-pc-ui、vue3-pc-ui）
  - 样式系统开发（packages/styles、configs/tailwind）
  - 配置包开发（configs/eslint、prettier、vite、rollup）
  - CLI 工具开发（cli/create-app、product-analysis、workflow-cloud）
  - 应用开发（apps/playground、yapi-to-typescript）

### 2. 【必选】规则声明
- **[ ] 明确遵循的规则文件清单**：
  - `project-knowledge.mdc` - 项目技术架构和业务背景
  - `development-workflow.mdc` - 开发工作流程（是否需要 spec）
  - `project-standards.mdc` - 代码规范和命名约定
  - `integration-standards.mdc` - 构建部署和集成规范
  - `quality-checklist.mdc` - 当前质量检查清单

### 3. 【必选】用户确认
- **[ ] 向用户确认场景识别和规则选择是否正确**
- **[ ] 确认开发任务的具体范围和预期结果**
- **[ ] 确认是否需要执行完整的 spec 工作流程**

### 4. 【必选】规则执行
- **[ ] 严格按照选定的规则文件进行开发**
- **[ ] 开发过程中遇到规则冲突时及时与用户沟通**

## ⚠️ 常见问题避免

### 开发流程问题
- **避免跳过场景识别直接开始开发**
  - 不同工作区的开发模式和要求差异很大
  - 组件库需要考虑 API 设计和向后兼容
  - CLI 工具需要考虑模板结构和参数设计
  - 配置包需要考虑版本兼容和插件集成

- **避免混用不同工作区的开发模式**
  - 不要将应用层的业务逻辑混入基础库
  - 不要在组件库中直接依赖具体的业务 API
  - 不要在配置包中硬编码特定项目的路径

### 技术架构问题
- **避免忽略 monorepo 的依赖关系**
  - 确保基础包优先构建（shared → types → utils → hooks）
  - 组件库要正确依赖基础包，不能循环依赖
  - 应用层可以依赖所有包，但不能被其他包依赖

- **避免违反类型安全原则**
  - 严禁跨层直接使用类型定义
  - API 层类型与业务层类型要通过适配器转换
  - 组件库的类型定义要独立且完整

### 性能和用户体验问题
- **避免忽略千万级用户的性能要求**
  - 组件必须支持虚拟滚动和懒加载
  - 避免不必要的重渲染和内存泄漏
  - 静态资源要优化压缩和缓存策略

- **重要技术方案建议与用户确认**
  - 新增组件的 API 设计
  - 重大的架构调整和依赖变更
  - 可能影响用户体验的性能优化方案

## 🔄 质量保障机制

### 发现问题时的处理流程
如发现开发不符合规范，可以：
- **[ ] 指出具体问题点**：明确违反了哪个规则或最佳实践
- **[ ] 要求重新执行规则检查流程**：回到开发前检查步骤
- **[ ] 明确指定需要遵循的规则文件**：避免规则理解偏差

### 代码审查检查点
- **[ ] TypeScript 类型检查**：`pnpm type:check` 无错误
- **[ ] ESLint 代码质量检查**：`pnpm lint` 无错误
- **[ ] 单元测试覆盖**：新增代码必须有对应测试
- **[ ] 构建验证**：`pnpm build` 所有相关包构建成功

## 📋 具体开发质量检查

### 基础库开发检查清单
- **[ ] 类型定义完整**：所有导出函数都有完整的 TypeScript 类型
- **[ ] JSDoc 注释**：公共 API 都有详细的使用说明
- **[ ] 单元测试**：覆盖率 > 80%，包含边界情况测试
- **[ ] 无副作用**：工具函数要纯函数设计
- **[ ] 版本兼容**：确保不破坏现有 API 的向后兼容性

### 组件库开发检查清单
- **[ ] API 设计合理**：Props、Events、Slots 设计遵循 Vue 3 最佳实践
- **[ ] 样式系统兼容**：支持主题切换（CSS Variables）
- **[ ] 响应式设计**：支持不同屏幕尺寸和设备
- **[ ] 无障碍访问**：符合 WCAG 2.1 基本要求
- **[ ] 演示页面**：在 playground 中添加完整的使用示例
- **[ ] 文档完整**：README.md 包含 API 文档和使用指南

### CLI 工具开发检查清单
- **[ ] 命令行接口**：支持 --help、--version 等标准参数
- **[ ] 错误处理**：提供清晰的错误信息和解决建议
- **[ ] 模板验证**：生成的文件结构和内容正确
- **[ ] 跨平台兼容**：在 Windows、macOS、Linux 下都能正常工作
- **[ ] 性能优化**：避免不必要的文件 I/O 和网络请求

### 配置包开发检查清单
- **[ ] 插件兼容性**：与 Vite 7、ESLint 9 等最新版本兼容
- **[ ] 配置合理性**：规则配置平衡严格性和实用性
- **[ ] 扩展性支持**：允许项目根据需要覆盖配置
- **[ ] 文档清晰**：配置项说明和使用示例完整

### 应用开发检查清单
- **[ ] 路由配置**：新页面的路由和菜单配置正确
- **[ ] 状态管理**：Pinia store 的设计合理，避免过度复杂
- **[ ] API 集成**：HTTP 请求的错误处理和缓存策略
- **[ ] 用户体验**：加载状态、错误提示、交互反馈完整
- **[ ] 性能优化**：懒加载、虚拟滚动等性能优化措施

## 🚀 发布前最终检查

### 构建和测试
- **[ ] 全量构建成功**：`pnpm build` 无错误
- **[ ] 测试全部通过**：`pnpm test` 无失败用例
- **[ ] 类型检查通过**：`pnpm type:check` 无类型错误
- **[ ] 代码质量检查**：`pnpm lint` 无警告和错误

### 版本和文档
- **[ ] 版本更新正确**：使用 Changesets 管理版本变更
- **[ ] 变更日志完整**：CHANGELOG.md 记录所有重要变更
- **[ ] 文档同步更新**：相关文档和示例与代码保持一致
- **[ ] 演示正常**：playground 中的演示功能正常工作

### 兼容性和性能
- **[ ] 向后兼容**：不破坏现有 API 和功能
- **[ ] 浏览器兼容**：支持目标浏览器版本
- **[ ] 性能基准**：没有明显的性能回归
- **[ ] 包大小合理**：构建产物大小在合理范围内

### 部署验证
- **[ ] 本地预览正常**：`pnpm preview` 功能正常
- **[ ] 静态部署验证**：GitHub Pages 部署后访问正常
- **[ ] CDN 资源**：静态资源路径和缓存策略正确

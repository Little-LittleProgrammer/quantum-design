---
alwaysApply: false
---
# quantum-design - 项目开发规范

## 目录结构规范

### 类型定义分层管理
- **全局类型**: 写在 `packages/types/` 目录下，按模块分类（global/、lib/、vue/）
- **组件库类型**: 写在 `packages/vue3-antd-pc-ui/src/types/` 和 `packages/vue3-pc-ui/src/types/` 目录下
- **应用页面类型**: 写在 `apps/playground/types/` 目录下
- **API接口类型**: 写在 `apps/playground/src/http/api/*/interface.ts` 文件中
- **业务状态类型**: 写在 `apps/playground/src/store/modules/` 各模块中

### 组件组织结构
- **公共组件**: `packages/vue3-antd-pc-ui/src/q-*/` 和 `packages/vue3-pc-ui/src/*/`
- **布局组件**: `apps/playground/src/layout/` 和 `apps/playground/src/components/layout/`
- **业务页面**: `apps/playground/src/views/` 按功能模块分组
- **工具组件**: 各组件库内部的 `components/` 子目录

### 样式文件结构
- **基础样式**: `packages/styles/base/` （CSS 重置、变量定义等）
- **主题样式**: `packages/styles/antd/` 和 `packages/styles/element/`
- **组件样式**: 各组件目录下的 `style/` 文件夹，使用 SCSS + CSS Variables

## 代码规范

### 类型安全与分层
- 严禁跨层直接使用类型，如页面组件不能直接使用API层的原始响应类型，需要通过适配层转换
- 组件库必须提供完整的 TypeScript 类型定义和导出
- API接口类型与前端业务类型分离，避免紧耦合
- 所有公共类型必须有 JSDoc 注释说明

### 代码注释与文档
- 新增的组件/函数/类型/常量上都要带上对应的用法说明和注释
- 组件库的每个组件必须包含 README.md 说明文档
- 复杂的业务逻辑要有注释说明设计思路
- 导出的工具函数必须包含完整的 JSDoc 类型注解

### 文件与组件管理
- 如非必要，勿增文件，勿增组件，不要新增没有使用的代码放在那里
- 如果已有的组件或函数满足需求，优先复用而非新建
- 当你完成所有改动内容后，需要重新回顾修改的代码是否符合规范
- 当你要新增文件时，需要查看该文件夹下之前其他文件的命名，参考其他文件的命名

### 路由与页面管理
- 当新增页面/路由时，你在完成页面代码之后，还需要更新路由配置等相关文件，确保新页面能正常访问
- 路由文件按功能模块分组在 `router/modules/` 下
- 菜单配置统一在 `menus/index.ts` 中管理
- 页面组件命名遵循 PascalCase，路由路径使用 kebab-case

## 性能优化要求

quantum-design 服务千万级用户，对性能要求极高：

### 组件层面优化
- 组件尽量使用缓存和懒加载，避免不必要的重复渲染
- 合理使用 `defineAsyncComponent` 进行组件懒加载
- 大型组件使用 `v-memo` 进行缓存优化
- 合理使用 `computed` 和 `watch` 避免无效计算

### 数据渲染优化
- 列表数据尽量使用虚拟滚动和分页加载，避免一次性渲染大量数据导致页面卡顿
- 表格组件优先使用 AntD Table 的虚拟滚动特性
- 大数据集使用增量加载和搜索过滤

### 资源加载优化
- 图片和静态资源要进行压缩和懒加载，提升页面加载速度
- 使用 Vite 的动态导入进行代码分割
- 静态资源使用 CDN 或合适的缓存策略

### 网络请求优化
- API请求要做防抖处理和错误重试，提升用户体验
- 合理使用请求缓存，避免重复请求
- 长列表使用无限滚动而非传统分页

### 开发时性能考虑
- 在写所有业务代码时，尽量都多考虑一些性能优化和用户体验问题
- 避免在 render 函数中进行复杂计算
- 合理使用 Pinia 状态管理，避免不必要的响应式数据

## 文件命名约定

### 组件文件命名
- Vue 组件文件：使用 PascalCase（如 `UserProfile.vue`）
- 组件目录：使用 kebab-case（如 `q-form/`、`q-table/`）
- 样式文件：与组件同名，使用 kebab-case（如 `user-profile.scss`）

### 工具文件命名
- 工具函数文件：使用 kebab-case（如 `date-utils.ts`）
- 配置文件：使用 kebab-case 或 camelCase（如 `vite.config.ts`）
- 类型定义文件：使用 kebab-case（如 `form-types.ts`）

### 常量与枚举命名
- 常量：使用 SCREAMING_SNAKE_CASE（如 `API_BASE_URL`）
- 枚举：使用 PascalCase（如 `StatusEnum`）
- 接口：使用 PascalCase 并以 I 开头（如 `IUserInfo`）

## 组件设计原则

### API 设计原则
- 组件 API 设计要简洁明了，遵循 Vue 3 Composition API 最佳实践
- Props 定义要完整，包括类型、默认值、验证规则
- 事件命名使用 kebab-case，如 `@value-change`
- 插槽使用语义化命名，提供 fallback 内容

### 样式设计原则
- 使用 CSS Variables 系统支持主题切换
- 样式组织遵循 BEM 命名规范
- 响应式设计使用 Tailwind CSS 断点系统
- 避免使用 !important，通过选择器优先级控制

### 可复用性原则
- 组件设计要考虑复用性，避免过度耦合业务逻辑
- 提供合理的配置项和扩展点
- 大型组件要支持插槽和作用域插槽
- 工具函数要纯函数设计，避免副作用

## monorepo 协作规范

### 包依赖管理
- 内部包引用使用 `workspace:*` 协议
- 外部依赖版本使用 catalog 统一管理
- 不同工作区的依赖版本要保持一致性
- peerDependencies 要明确声明版本范围

### 构建与发布
- 每个包都要配置正确的 `exports` 字段
- 库包要同时支持 ESM 和 CommonJS 格式
- 版本发布使用 Changesets 管理
- 发布前确保所有依赖包已正确构建

### 测试覆盖
- 每个工具函数都要有对应的单元测试
- 组件要有基础的渲染测试和交互测试
- 集成测试在 playground 中验证
- 维持合理的测试覆盖率（目标 > 80%）

---
alwaysApply: false
---
# quantum-design - 开发集成规范

## 文档生成规则

### 项目文档
1. 生成项目后创建或更新 README.md 文件，包含：
   - 项目基本信息（名称、描述、版本）
   - 技术架构和工作区结构说明
   - 安装和使用方法
   - 涉及的依赖关系和版本要求
   - 构建和发布流程
   - 让维护者可以参考来进行修改和维护

### 组件文档
2. 组件库新增组件时必须同时生成：
   - 组件的 README.md 说明文档
   - 在 playground 中添加演示页面
   - API 文档包含 Props、Events、Slots、Methods 说明
   - 使用示例和最佳实践

### API 文档
3. CLI 工具和配置包要提供：
   - 命令行工具的 --help 文档
   - 配置项的详细说明和示例
   - 常见问题和故障排除指南

## 配置文件规则

### 包配置管理
1. 每个新包都要配置完整的 package.json：
   - 正确的 name、version、description
   - 完整的 exports 字段配置
   - 合适的 peerDependencies 声明
   - scripts 脚本配置（build、test、dev等）

### 元数据文件
2. 为了方便其他不使用 AI 的人了解有哪些资源，在生成之后同时生成：
   - quantum.config.json（项目元数据配置）
   - 包含工作区信息、依赖关系、构建配置等

```json
{
  "name": "quantum-design",
  "version": "3.0.0-beta.0",
  "type": "monorepo",
  "workspaces": {
    "apps": ["playground", "yapi-to-typescript"],
    "packages": ["utils", "shared", "types", "hooks", "styles", "vue3-antd-pc-ui", "vue3-pc-ui"],
    "configs": ["eslint", "prettier", "vite", "rollup", "tailwind"],
    "cli": ["create-app", "product-analysis", "workflow-cloud"]
  },
  "technologies": {
    "framework": "Vue 3.5",
    "build": "Vite 7 + Turbo",
    "package-manager": "pnpm",
    "ui": "Ant Design Vue + Custom Components"
  }
}
```

## 构建与部署规范

### Turbo 构建配置
1. 使用 Turbo 并行构建提升效率：
   - 基础包优先构建：shared → types → utils → hooks
   - 样式包并行构建：styles、tailwind
   - 组件库按依赖顺序：vue3-pc-ui → vue3-antd-pc-ui
   - 应用最后构建：playground、工具应用

### 包发布流程
2. 使用 Changesets 管理版本和发布：
   - 开发完成后运行 `pnpm changeset` 添加变更记录
   - 合并到主分支前运行 `pnpm changeset version` 更新版本
   - CI/CD 自动执行 `pnpm changeset publish` 发布到 npm

### 静态部署
3. Playground 应用部署到 GitHub Pages：
   - 构建产物部署到 `gh-pages` 分支
   - 访问地址：https://little-littleprogrammer.github.io/quantum-design/playground/
   - 确保静态资源路径正确配置

## 开发环境集成

### IDE 配置
1. 项目根目录配置：
   - `.vscode/settings.json` - VS Code 工作区配置
   - `.cursor/rules/` - Cursor AI 规则配置
   - `.editorconfig` - 编辑器配置统一

### Git 工作流
2. 提交规范集成：
   - 使用 Husky + lint-staged 进行 Git hooks
   - 提交前自动运行 ESLint 和 Prettier
   - 使用 czg 工具进行规范化提交
   - Commitlint 验证提交信息格式

### 环境变量管理
3. 不同环境的配置管理：
   - 开发环境：`.env.development`
   - 生产环境：`.env.production`
   - Tauri 环境：通过 `TAURI_ENV_PLATFORM` 区分
   - 使用 Vite 的环境变量系统

## 第三方集成规范

### Sentry 错误监控
1. 集成 Sentry 进行错误追踪：
   - 在 `configs/sentry` 中统一配置
   - Playground 应用集成 Sentry Vue 插件
   - 构建时自动上传 source maps

### Tauri 桌面应用
2. 支持 Tauri 桌面端集成：
   - `src-tauri/` 目录包含 Rust 配置
   - 使用 `pnpm tauri:dev` 开发桌面应用
   - 使用 `pnpm tauri:build` 构建安装包

### Monaco Editor 集成
3. 代码编辑器集成：
   - 在 playground 中演示 Monaco Editor
   - 支持 TypeScript 语言服务
   - 自定义主题与项目主题系统同步

## 测试集成规范

### 单元测试
1. 使用 Vitest 进行单元测试：
   - 每个工具函数都要有对应测试
   - 组件测试使用 @vue/test-utils
   - 测试覆盖率目标 > 80%

### 集成测试
2. 在 Playground 中进行集成验证：
   - 新组件必须在 playground 中添加演示
   - 验证组件在实际环境中的表现
   - 测试主题切换、响应式等特性

### E2E 测试
3. 关键功能的端到端测试：
   - CLI 工具的文件生成功能
   - 组件库的核心交互功能
   - 构建和发布流程验证

## 版本兼容性管理

### 依赖版本同步
1. 使用 catalog 统一管理外部依赖版本：
   - 确保所有工作区使用相同版本的基础依赖
   - 及时更新到最新稳定版本
   - 处理版本冲突和不兼容问题

### API 版本策略
2. 遵循语义化版本规范：
   - 主版本：不兼容的 API 修改
   - 次版本：向下兼容的功能性新增
   - 修订版本：向下兼容的问题修正

### 迁移指南
3. 重大版本更新时提供：
   - 详细的迁移指南文档
   - Breaking Changes 清单
   - 自动化迁移工具（如适用）

## 性能监控集成

### 构建性能
1. 监控构建时间和包大小：
   - 使用 Turbo 缓存优化构建速度
   - 监控各包的构建产物大小
   - 及时发现性能回归

### 运行时性能
2. 集成性能监控工具：
   - 在 playground 中集成性能分析
   - 监控组件渲染性能
   - 追踪内存使用情况

### 用户体验监控
3. 关注用户体验指标：
   - 页面加载时间
   - 交互响应时间
   - 错误率和稳定性指标
